{
  "sections": [
    {
      "id": "hashes",
      "name": {
        "ru": "Хэши",
        "en": "Hashes"
      },
      "functions": [
        {
          "id": "sha256",
          "name": {
            "ru": "SHA-256",
            "en": "SHA-256"
          },
          "metadata": {
            "author": "oxxx1mif",
            "crypto_version": "v0.1",
            "os_version": "v0.7"
          },
          "description": {
            "ru": "Криптографическая хэш-функция, вычисляющая 256-битное значение по алгоритму SHA-2.",
            "en": "Cryptographic hash function that computes a 256-bit digest using the SHA-2 algorithm."
          },
          "tags": ["sha2", "hashing", "integrity", "fips"],
          "code_snippet": "// Rust API\nuse sha256::Sha256;\n\nlet mut hasher = Sha256::new();\nhasher.update(b\"hello world\");\nlet digest = hasher.finalize();\nassert_eq!(digest.len(), 32);\n\n// C API\n#include <stdint.h>\n#include <string.h>\n\nstruct Sha256;\n\nextern void sha256_init(struct Sha256 *ctx);\nextern void sha256_update(struct Sha256 *ctx, const uint8_t *data, size_t len);\nextern void sha256_finalize(struct Sha256 *ctx, uint8_t out[32]);\n\nvoid compute() {\n    struct Sha256 ctx;\n    sha256_init(&ctx);\n    sha256_update(&ctx, (const uint8_t*)\"abc\", 3);\n    uint8_t digest[32];\n    sha256_finalize(&ctx, digest);\n}",
          "implementation_url": "https://github.com/NightFox-YT/Realix/tree/feature/crypto/source/crypto/sha256",
          "contributors": ["oxxx1mif"]
        }
      ]
    }
  ]
}
